// Simple test program for plasm run command
fn add(u64 a, u64 b) u64 {
  return a + b;
}

fn fib(u64 n) u64 {
  if (n <= 1) {
    return n;
  }
  return fib(n - 1) + fib(n - 2);
}

pub proc $main() void {
  final result = add(10, 20);
  final fib10 = fib(10);
}
